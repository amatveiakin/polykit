#include "lib/gamma.h"

#include "gtest/gtest.h"


// bool are_weakly_separated_naive(const Gamma& g1, const Gamma& g2) {
//   const auto a = bitset_difference(g1.index_bitset(), g2.index_bitset());
//   const auto b = bitset_difference(g2.index_bitset(), g1.index_bitset());
//   static_assert(a.size() == b.size());
//   constexpr int n = a.size();
//   for (const auto& [x1,y1,x2,y2] : cartesian_poser(seq_incl(n)) {
//   }
//   // ...
// }


// TEST(GammaTest, WeaklySeparated) {
// }
